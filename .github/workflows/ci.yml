name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [18, 20, 22]
    name: node ${{ matrix.node }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm test
      - name: VS Code extension — typecheck and bundle
        run: |
          npm run typecheck -w react-a11y-vscode
          npm run build -w react-a11y-vscode
      - name: Dogfood — scan the example projects
        run: |
          node packages/cli/dist/index.js examples/web-demo --fail-on none
          node packages/cli/dist/index.js examples/native-demo --fail-on none
          node packages/cli/dist/index.js examples/native-violations --fail-on none
